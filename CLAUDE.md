@AGENTS.md

# Projet ZURI

## Stack
- Next.js 16.2.10 (App Router, Turbopack)
- TypeScript uniquement — pas de fichiers JavaScript
- TailwindCSS v4 (via `@tailwindcss/postcss`, config dans `postcss.config.mjs`)
- ESLint (`eslint.config.mjs`)

## Commandes
- `npm run dev` — serveur de développement (http://localhost:3000)
- `npm run build` — build de production
- `npm run lint` — lint

## Git / GitHub
- Dépôt distant : https://github.com/contactzuripro-ctrl/ZURI (branche `main`)
- Nom du package npm : `zuri` (minuscules imposées par npm, le dossier reste `ZURI`)
- Ne jamais commiter de tokens ou secrets dans ce dépôt

## Règles de travail (obligatoires)
1. **Documenter chaque feature** : avant/pendant chaque feature, ajouter une entrée dans le « Journal des features » ci-dessous (quoi, où, quels composants).
2. **Découper en composants** : jamais de page monolithique. Chaque bloc visuel ou logique réutilisable = un composant dans son propre fichier.
3. **Nommage lisible par un humain** :
   - Composants : `PascalCase` (ex. `ProductCard.tsx`), un composant par fichier, le fichier porte le nom du composant.
   - Fonctions / variables : `camelCase`, noms explicites (`fetchUserOrders`, pas `getData`).
   - Dossiers : `kebab-case` ou nom de domaine clair (`user-profile/`, `checkout/`).
   - Types partagés : dans `src/types/`, interfaces en `PascalCase`.
4. **Code repris par un humain** : props typées explicitement, pas de logique cachée, commentaire uniquement quand une contrainte n'est pas évidente dans le code.

## Structure des dossiers
```
src/
  app/                 # Routes Next.js (App Router) — uniquement pages/layouts
  components/
    ui/                # Composants génériques réutilisables (Button, Card, Input…)
    layout/            # Header, Footer, Sidebar…
  features/            # Un dossier par feature métier (ses composants + sa logique)
  hooks/               # Hooks React personnalisés (useXxx.ts)
  lib/                 # Fonctions utilitaires, clients API
  types/               # Types et interfaces TypeScript partagés
```
Règle : une feature vit dans `src/features/<nom-feature>/`. Les pages dans `src/app/` ne font qu'assembler les composants des features.

## Journal des features
> Chaque feature ajoutée au projet est consignée ici : nom, date, description, fichiers/composants créés.

| Date | Feature | Description | Fichiers principaux |
|------|---------|-------------|---------------------|
| 2026-07-10 | Initialisation | Projet Next.js + TypeScript + Tailwind, structure de dossiers, conventions | `src/app/`, `CLAUDE.md` |
| 2026-07-10 | Layout dashboard | Sidebar prune fixe (logo Zuri, 9 liens + Paramètres, lien actif surligné), en-tête de page, palette Zuri (prune/crème/or) dans Tailwind | `src/components/layout/` (`Sidebar`, `SidebarLink`, `navigation`, `PageHeader`), `src/app/globals.css`, `src/app/layout.tsx` |
| 2026-07-10 | Composants UI | Carte, carte de statistique, badge de statut, tableau générique, bouton principal | `src/components/ui/` (`Card`, `StatCard`, `StatusBadge`, `DataTable`, `PrimaryButton`) |
| 2026-07-10 | 01 Tableau de bord | Recette du jour, RDV du jour, nouvelles clientes, alertes stock + liste des prochains RDV | `src/app/page.tsx`, `src/features/tableau-de-bord/` |
| 2026-07-10 | 02 Agenda | Planning du jour en colonnes par employée, cartes de RDV triées par heure | `src/app/agenda/`, `src/features/agenda/` |
| 2026-07-10 | 03 Clients | Liste des clientes : téléphone, prestation favorite, visites, total dépensé, dernière visite | `src/app/clients/`, `src/features/clients/` |
| 2026-07-10 | 04 Paiements | 3 indicateurs (encaissé, acomptes, transactions) + tableau des transactions Orange Money / Wave / Espèces avec statuts | `src/app/paiements/`, `src/features/paiements/` |
| 2026-07-10 | 05 Comptabilité | CA, dépenses, bénéfice net du mois + dernières dépenses + bouton export | `src/app/comptabilite/`, `src/features/comptabilite/` |
| 2026-07-10 | 06 Prestations | Catalogue en grille : catégorie, prix, durée, promotions | `src/app/prestations/`, `src/features/prestations/` |
| 2026-07-10 | 07 Employés | Cartes par employée : objectif mensuel, barre de progression, commission calculée | `src/app/employes/`, `src/features/employes/` |
| 2026-07-10 | 08 Stock | Inventaire avec seuils d'alerte et statuts En stock / À commander / Rupture | `src/app/stock/`, `src/features/stock/` |
| 2026-07-10 | 09 Marketing | Indicateurs fidélité + tableau des campagnes SMS / WhatsApp (envois, taux de réponse) | `src/app/marketing/`, `src/features/marketing/` |
| 2026-07-10 | Paramètres | Écran d'infos du salon (nom, devise, moyens de paiement) — à compléter | `src/app/parametres/` |
| 2026-07-10 | Design Neumorphism | Refonte du contenu en soft UI : surfaces couleur du fond, relief par double ombre (`shadow-neu`, `shadow-neu-sm`), éléments actifs/creusés en ombre interne (`shadow-neu-inset`, `shadow-neu-inset-sm`) — badges, boutons (enfoncés au clic), barres de progression, cartes RDV | `src/app/globals.css` (tokens d'ombres), `src/components/ui/`, features |
| 2026-07-10 | Menu Material + Organic | Sidebar façon navigation drawer M3 (remplace un essai glassmorphism supprimé) : surface prune unie avec élévation, item actif = pilule pleine or, survol = state layer `bg-white/10`. Touche Organic : bord droit très arrondi (`rounded-r-[2.5rem]`), logo et pilules aux coins asymétriques (border-radius irréguliers « galet »), 2 blobs décoratifs en fond | `src/components/layout/` (`Sidebar`, `SidebarLink`), `src/app/layout.tsx` |

| 2026-07-10 | Bouton ouvrir/fermer le menu | Bouton neumorphique dans l'en-tête de chaque page qui replie/déplie la sidebar (animation 300 ms). État partagé via contexte React (`SidebarProvider` dans le layout racine, hook `useSidebar`) | `src/components/layout/` (`SidebarContext`, `SidebarToggle`, `PageHeader`, `Sidebar`), `src/app/layout.tsx` |

| 2026-07-10 | Organic Design du contenu | Touche organique sur le contenu (en plus du neumorphism) : cartes, boutons, bouton de menu, cartes RDV et avatars en coins asymétriques « galet » ; 3 blobs or/prune très discrets fixés derrière le contenu (`OrganicBackdrop`) | `src/components/ui/` (`Card`, `PrimaryButton`), `src/components/layout/` (`OrganicBackdrop`, `SidebarToggle`), features agenda/employés |

| 2026-07-10 | Vrai calendrier Agenda | Remplace les simples colonnes par un calendrier complet : vue **Mois** (grille lun→dim, pastilles de RDV colorées par employée, « +n autres », jour courant surligné or, clic sur un jour → sa journée) et vue **Jour** (grille horaire 08:00–19:00, une colonne par employée, blocs positionnés selon l'horaire). Navigation ‹ › + « Aujourd'hui » + bascule Jour/Mois. Le type `Appointment` a maintenant un champ `date` | `src/features/agenda/` (`Calendar`, `CalendarHeader`, `MonthView`, `DayView`, `data.ts`), `src/lib/calendar.ts`, `src/types/index.ts` — supprime `DailyPlanning`, `AppointmentCard` |

| 2026-07-10 | Refonte Minimalisme | Le contenu passe du Neumorphism au minimalisme façon Apple : fond blanc, cartes gris très clair sans ombre, badges = point coloré + texte, tableaux à fines lignes, typo mise en avant (grandes valeurs, libellés discrets), espacements élargis. Suppression des blobs (`OrganicBackdrop`) et formes « galet » du contenu. **Le menu garde son style Material + Organic** (validé par l'utilisateur) | `src/app/globals.css` (tokens neutres `surface`/`hairline`, suppression ombres neu), `src/components/ui/`, pages et features |
| 2026-07-10 | Barre de recherche animée | Dans l'en-tête de chaque page : fin anneau statique en dégradé prune→or, icône loupe en pastille or, halo doré + élargissement (w-64 → w-96) au focus. Tant que le champ est vide, les invites se tapent **à la machine à écrire** (lettre par lettre, pause, effacement, invite suivante) avec curseur doré clignotant — hook `useTypewriter` dans le composant. Une version très voyante (anneau défilant + halo pulsant) a été jugée trop voyante et retirée. Recherche non branchée (UI seule) | `src/components/layout/SearchBar.tsx`, `PageHeader.tsx`, utilitaires `search-ring`/`animate-cursor-blink` dans `globals.css` |

| 2026-07-10 | Mode nuit | Bouton « Mode nuit / Mode jour » (lune/soleil) en bas du menu, au-dessus de Paramètres. Bascule la classe `dark` sur `<html>` : toutes les couleurs neutres du contenu sont des variables CSS redéfinies dans le bloc `.dark` de `globals.css` (fond `#131315`, cartes `#1d1d20`, textes inversés, statuts éclaircis). Choix persisté dans `localStorage` (`zuri-theme`) et réappliqué avant le premier rendu par un script inline dans `<head>` (pas de flash). Source de vérité = classe du `<html>` lue via `useSyncExternalStore` + MutationObserver | `src/components/layout/ThemeToggle.tsx`, `Sidebar.tsx`, `src/app/layout.tsx`, `globals.css` (tokens `page`/`surface`/`elevated`/`hairline`) |

| 2026-07-10 | Accent rose poudré | Le doré est remplacé par du rose poudré (#E39BB1) partout : pilule active du menu, logo, pastille et curseur de la barre de recherche, dégradé de l'anneau, couleur d'Awa dans le calendrier. Le token est renommé `accent-400/500` : pour changer de couleur d'accent, ne modifier que ces 2 variables dans `globals.css` | `src/app/globals.css`, `Sidebar`, `SidebarLink`, `SearchBar`, `features/agenda/data.ts` |

| 2026-07-10 | États rose du menu | Les liens du menu réagissent en rose : survol = voile rose 25 % + texte rose, appui = rose 50 %, et **le lien cliqué devient la pilule rose pleine immédiatement** (sélection optimiste dans `Sidebar` : état `clickedHref` prioritaire sur `usePathname` jusqu'à ce que l'URL confirme) | `src/components/layout/Sidebar.tsx`, `SidebarLink.tsx`, `ThemeToggle.tsx` |

| 2026-07-10 | Menu déroulant de la recherche | Au focus sur la barre de recherche, un panneau s'ouvre en dessous listant les pages du menu (icône + nom) ; la saisie filtre la liste, cliquer navigue et referme. Animation d'ouverture : fondu + glissement + zoom du panneau (220 ms) et apparition en cascade des éléments (35 ms de décalage chacun). Fermeture au clic extérieur (hook `useCloseOnOutsideClick`) et à Échap. Vérifié au navigateur (Playwright) : ouverture, filtre « sto » → Stock, navigation OK | `src/components/layout/SearchBar.tsx` (réutilise `navigation.ts`), keyframes `dropdown-in`/`dropdown-item-in` dans `globals.css` |

| 2026-07-10 | Retour de l'Organic Design (contenu) | L'Organic Design revient **en plus** du minimalisme (il avait été retiré par erreur lors de la refonte) : cartes, boutons d'action, bouton de menu, panneau de recherche, avatars et blocs de RDV du calendrier en coins asymétriques « galet » ; 3 blobs rose/prune très discrets derrière le contenu (`OrganicBackdrop`, compatible mode nuit). Le style du contenu est donc : Minimalisme + touches organiques | `src/components/ui/` (`Card`, `PrimaryButton`), `src/components/layout/` (`OrganicBackdrop`, `SidebarToggle`, `SearchBar`), `src/features/agenda/DayView.tsx`, `src/app/layout.tsx` |

| 2026-07-10 | Cartes en verre (glassmorphism) | Les box du dashboard perdent leur gris : fond très translucide (`--glass-bg` à 16 %), flou d'arrière-plan `backdrop-blur(32px) saturate(200%)`, **reflet diagonal** sur la vitre (dégradé 135°), bords lumineux (bordure blanche 95 % + double reflet intérieur), ombre portée profonde. Utilitaire `glass` dans `globals.css`, variables adaptées au mode nuit. Les bulles d'arrière-plan sont elles-mêmes floutées (`blur-3xl`, opacités 15-30 %) — nappes douces qui alimentent la transparence sans être voyantes. Intensité validée par l'utilisateur après 2 itérations | `src/app/globals.css` (utilitaire `glass` + variables `--glass-*`), `src/components/ui/Card.tsx`, `OrganicBackdrop.tsx` |

| 2026-07-10 | Menu en verre | La sidebar devient une vitre prune translucide : `bg-plum-900/45` + `backdrop-blur-2xl`, bords lumineux (bordure droite blanche 25 % + reflets intérieurs haut/droite), bord droit arrondi conservé. Des halos rose/prune floutés (`blur-3xl`) sur fond `plum-950` sont fixés **derrière** la vitre (dans le composant, en `-z-10`) et suivent l'ouverture/fermeture du menu. Garde le Material + Organic (pilule rose optimiste, formes galet, blobs) | `src/components/layout/Sidebar.tsx` |

| 2026-07-10 | Animation de changement de page (fade-up en cascade) | À chaque navigation, les composants montent en fondu (fade-up) **en cascade** : l'en-tête d'abord, puis chaque bloc du contenu avec 80/170/260/350 ms de délai. Implémenté via `src/app/template.tsx` (remonté par Next.js à chaque route) + keyframes `fade-up` et sélecteurs `.animate-page-in > div > *` dans `globals.css` — aucune page à modifier. Vérifié au navigateur (opacité 0 au remontage → 1) | `src/app/template.tsx`, `globals.css` |
| 2026-07-10 | Page Clients maître-détail + photos | Refonte selon maquette : liste à gauche (recherche filtrante, avatars **photos réelles** dans `public/avatars/`, dernière visite), fiche à droite (photo, ancienneté, badge « Cliente fidèle », historique des prestations, notes privées, photos avant/après avec bouton +). Sélection au clic (état local), portraits libres choisis visuellement (femmes noires, clientèle ivoirienne), repli sur initiales si pas de photo. Remplace l'ancien tableau `ClientsTable` | `src/features/clients/` (`ClientsDirectory`, `ClientListPanel`, `ClientDetailPanel`, `ClientAvatar`, `data.ts`), `src/app/clients/page.tsx`, `src/lib/format.ts` (`formatDayMonth`, `formatMonthYear`), `public/avatars/` |
| 2026-07-10 | Correctif : panneau de recherche au-dessus du contenu | Le panneau du menu de recherche passait **sous** les cartes en verre (translucides), d'où une impression de transparence. Cause : l'animation de page (`fill-mode: both`) laisse un `transform` résiduel sur l'en-tête et chaque bloc → chacun devient un contexte d'empilement et les blocs suivants se peignent par-dessus. Correctif : keyframe `fade-up` termine sur `transform: none` et l'en-tête reçoit `position: relative; z-index: 40`. Vérifié au navigateur (Playwright, `elementFromPoint`) en modes jour et nuit | `src/app/globals.css` |
| 2026-07-10 | Filtre par employée dans l'Agenda | Dans la barre du calendrier, entre le titre et la bascule Jour/Mois : pilule « Toutes » + pastille photo par employée. Cliquer une photo n'affiche que ses RDV (anneau à sa couleur, autres pastilles estompées) ; re-cliquer ou « Toutes » réaffiche tout. Le filtrage se fait en amont dans `Calendar` (les deux vues en profitent) ; en vue Jour, seule la colonne de l'employée filtrée s'affiche (pleine largeur, colonnes dynamiques via `gridTemplateColumns`). `CalendarHeader` accepte maintenant des `children`. Vérifié au navigateur (Playwright) : Mois 12 → 6 pastilles avec Awa, Jour 3 → 1 colonne, désélection OK | `src/features/agenda/` (`EmployeeFilter` nouveau, `Calendar`, `CalendarHeader`, `DayView`, `data.ts` : `employeeRingColors`) |

| 2026-07-10 | Photos de profil partout | 7 nouveaux portraits (femmes noires, vérifiés visuellement, stockés dans `public/avatars/`) : les 4 clientes des « Rendez-vous à venir » du tableau de bord, les 3 employées (page Employés + en-têtes de colonnes de la vue Jour de l'agenda). `ClientAvatar` généralisé en `Avatar` partagé (`src/components/ui/Avatar.tsx`) : photo ou initiales en repli, forme galet. Champs `clientPhotoUrl` (Appointment) et `photoUrl` (Employee) ajoutés aux types | `src/components/ui/Avatar.tsx`, `src/types/index.ts`, features tableau-de-bord/employes/agenda (+ `employeePhotos`), `public/avatars/` |

| 2026-07-10 | Photos dans le tableau Paiements | La colonne « Cliente » du tableau des paiements affiche la photo de profil (composant `Avatar` partagé) à côté du nom — mêmes photos que la page Clients. Champ `clientPhotoUrl` ajouté au type `Payment` | `src/features/paiements/` (`PaymentsTable`, `data.ts`), `src/types/index.ts` |

| 2026-07-10 | Logos des moyens de paiement | Dans la colonne « Méthode » du tableau des paiements, les icônes lucide génériques sont remplacées par les vrais logos : carré Orange (SVG officiel, Wikimedia) pour Orange Money, pingouin (favicon officiel wave.com) pour Wave ; les espèces gardent un billet lucide sur pastille verte. Tuiles 24 px arrondies via `next/image`, libellé unifié `text-ink-600`. Vérifié au navigateur (Playwright) | `src/features/paiements/PaymentMethodLabel.tsx`, `public/logos/` (`orange-money.svg`, `wave.png`) |

| 2026-07-10 | Responsive mobile | En dessous de `lg` (1024 px), le menu devient un **tiroir en superposition** : fermé par défaut, ouvert par le bouton de l'en-tête, voile prune flouté cliquable derrière (z-[45], au-dessus des en-têtes z-40), fermeture à la navigation et au clic sur le voile ; le desktop garde la colonne repliable. `SidebarContext` porte 2 états (`isOpen` desktop, `isMobileOpen` mobile) et `toggle()` choisit via `matchMedia`. Contenu : paddings `px-5 sm:px-10` sur les 10 pages, titre et en-tête compactés, barre de recherche pleine largeur sur mobile (panneau aligné dessus), vue Mois de l'agenda scrollable horizontalement (`min-w-140`), lignes de RDV du tableau de bord en `flex-wrap`. Vérifié au navigateur (Playwright, 390×844 et 1440×900) : 11 contrôles OK, aucun débordement horizontal | `src/components/layout/` (`SidebarContext`, `Sidebar`, `SidebarToggle`, `PageHeader`, `SearchBar`), pages `src/app/*/page.tsx`, `src/features/agenda/MonthView.tsx`, `src/features/tableau-de-bord/UpcomingAppointments.tsx` |

| 2026-07-10 | Modales des boutons d'action | Les 8 boutons d'en-tête ouvrent une modale au design du contenu (UI seule : « Annuler »/valider referment sans rien enregistrer, en attendant la vraie API). Panneau en **verre renforcé** (`glass-strong` : recette `glass` avec fond plus opaque pour la lisibilité) aux coins galet, voile prune flouté, animation fondu + montée + zoom, fermeture par Échap / clic extérieur / ✕, scroll de la page bloqué. **Rendu via un portail React vers `<body>`** : l'en-tête animé (keyframe `transform`, `fill-mode: both`) devient sinon le containing block du `position: fixed` et la modale se centre sur lui au lieu de l'écran. Champs réutilisables `FormField` (avec suffixe « F CFA »/« min »/« % »), `SelectField`, `TextareaField`, pied `ModalFooter` ; `PrimaryButton` accepte `onClick` et `type="submit"`. Un composant client « bouton + modale » par feature (`NewAppointmentAction`, `NewClientAction`, `PaymentLinkAction`, `ExportMonthAction`, `NewServiceAction`, `NewEmployeeAction`, `NewProductAction`, `NewCampaignAction`), les pages restent des composants serveur. Vérifié au navigateur (Playwright) : 8 ouvertures, fermetures Échap/valider, centrage, modes jour et nuit | `src/components/ui/` (`Modal`, `FormField`, `SelectField`, `TextareaField`, `ModalFooter`, `PrimaryButton`), `src/features/*/…Action.tsx`, pages `src/app/*/page.tsx`, `globals.css` (`glass-strong`, `animate-modal-*`) |

| 2026-07-10 | Photo de profil dans les modales | Champ « Photo de profil » en tête des modales Nouvelle cliente et Nouvelle employée : pastille galet cliquable en pointillés (icône appareil photo) + bouton « Choisir une photo » ; l'image choisie s'affiche en aperçu dans la pastille (`URL.createObjectURL`, aperçu local seulement — rien n'est envoyé) et le bouton devient « Changer la photo ». Vérifié au navigateur (Playwright, sélection d'un fichier réelle) | `src/components/ui/PhotoField.tsx`, `src/features/clients/NewClientAction.tsx`, `src/features/employes/NewEmployeeAction.tsx` |

| 2026-07-10 | Agenda mobile : vue Jour seule | Sous 640 px (`sm`), l'agenda n'a plus que la vue Jour : la grille du mois et la bascule Jour/Mois disparaissent (bascule cachée en CSS, vue forcée via le hook `useIsMobile` — `matchMedia` + `useSyncExternalStore` ; navigation ‹ › jour par jour, titre = jour courant). La vue Jour elle-même abandonne la grille horaire sur mobile au profit d'une **liste verticale de cartes** triées par heure (photo de l'employée, horaire, cliente, prestation, couleur de l'employée) — plus aucun tableau à défilement horizontal. Le filtre par employée continue de filtrer la liste ; état vide « Aucun rendez-vous ce jour ». Desktop inchangé. Vérifié au navigateur (Playwright, 390×844 et 1440×900) : mobile sans bascule ni grille ni débordement, 6 cartes triées, filtre Awa → 2, desktop Mois/Jour intacts, redimensionnement mobile→desktop OK | `src/hooks/useIsMobile.ts` (nouveau), `src/features/agenda/` (`Calendar`, `CalendarHeader`, `DayView`) |

| 2026-07-10 | Prestations réalisées (fiche cliente) | La section « Photos avant / après » de la fiche cliente devient **« Prestations réalisées »** : les blocs de couleur sont remplacés par de vraies photos de coiffures afro (tresses box braids, chignon tressé, bantu knots, tresses collées, tresses colorées, tresses aux coquillages…) affichées en vignettes 112 px via `next/image` (`fill` + `object-cover`). 9 photos libres (Wikimedia Commons / Flickr via Openverse) choisies visuellement et stockées dans `public/prestations/` ; retour utilisateur appliqué : les portraits où l'on voyait surtout le visage (Marie Diallo) remplacés par des photos centrées sur les cheveux. Champ `photoColors` → `servicePhotoUrls` dans `ClientProfile`. Vérifié au navigateur (Playwright) sur les 4 clientes | `src/features/clients/` (`ClientDetailPanel`, `data.ts`), `public/prestations/` |

| 2026-07-10 | Refonte page Marketing (maquette) | La page Marketing suit la maquette fournie : 3 indicateurs (Messages envoyés du mois 1840, Taux d'ouverture 64 %, Coupons utilisés 57) puis **liste verticale de cartes de campagnes** (remplace le tableau) : pastille d'icône par type (WhatsApp verte, SMS prune, cadeau or pour la fidélité, calendrier si programmée), nom + description, pilule de statut colorée (Active / Permanente / Programmée) et **bouton « Lancer »** par campagne (un essai « Modifier » + corbeille a été rejeté par l'utilisateur : le besoin est de *lancer* des campagnes). « Lancer » ouvre une modale de confirmation (cible « Envoyer à », bouton « Lancer maintenant ») — UI seule, rien n'est envoyé. Type `Campaign` refondu (`channel` whatsapp/sms/fidelite, `description`, `status` active/permanente/programmee). Supprime `CampaignsTable` et `LoyaltySummaryCards`. Vérifié au navigateur (Playwright, 1440×900 et 390×844, modes jour/nuit) : indicateurs, 4 cartes, 4 boutons « Lancer », modale ouverte/refermée (validation et Échap), aucun débordement | `src/features/marketing/` (`MarketingSummaryCards`, `CampaignList`, `CampaignCard`, `LaunchCampaignAction`, `data.ts`), `src/app/marketing/page.tsx`, `src/types/index.ts` |

| 2026-07-10 | Prestations : filtres + tableau (maquette) | La page Prestations passe de la grille de cartes à la disposition de la maquette : pilules de filtre par catégorie (« Toutes » + catégories dérivées des données, pilule active prune pleine, re-clic = désélection) au-dessus d'un tableau en carte de verre (Prestation, Catégorie, Durée, Prix, Promotion en pastille verte ou « – »). Durées formatées via `formatDuration` (150 → « 2h30 »). Catégorie « Soin » renommée « Soins ». Supprime `ServiceCard`. Vérifié au navigateur (Playwright, 1440×900 et 390×844) : filtres et désélection OK, aucun débordement mobile | `src/features/prestations/` (`ServicesCatalog`, `CategoryFilter` nouveau, `ServicesTable` nouveau, `data.ts`), `src/lib/format.ts` (`formatDuration`) |

| 2026-07-10 | Photos des prestations (catalogue) | Chaque prestation du catalogue affiche une photo : vignette 44 px (`size-11`, coins `rounded-xl`) à côté du nom dans le tableau. Champ `photoUrl` **obligatoire** sur le type `Service` — règle utilisateur : **toute nouvelle prestation doit avoir sa photo** dans `public/prestations/`. 4 nouvelles photos libres (Wikimedia Commons / Openverse, dont un salon ivoirien) recadrées sur le sujet pour rester lisibles en vignette : `coupe-brushing.jpg`, `defrisage-soin.jpg`, `manucure.jpg`, `vernis-semi-permanent.jpg` ; box braids, tresses collées et coloration réutilisent des photos existantes. La modale « Nouvelle prestation » gagne un champ « Photo de la prestation » (`PhotoField` généralisé avec une prop `shape` : « galet » pour les profils, « arrondi » pour les prestations). Vérifié au navigateur (Playwright, 1440×900 et 390×844) : 7 vignettes chargées, aucun débordement | `src/features/prestations/` (`ServicesTable`, `NewServiceAction`, `data.ts`), `src/components/ui/PhotoField.tsx`, `src/types/index.ts`, `public/prestations/` |

| 2026-07-10 | Responsive tablette (iPad) | Entre 1024 et 1279 px (iPad Pro portrait), le layout desktop s'activait (`lg`) mais la sidebar de 256 px ne laissait que ~700 px : textes des cartes compressés mot à mot. Correctif : les dispositions denses passent de `lg` à `xl` — StatCards ×3 (Paiements, Comptabilité, Marketing) en `sm:grid-cols-2 xl:grid-cols-3`, cartes Employés en `md:grid-cols-2 xl:grid-cols-3`, maître-détail Clients empilé jusqu'à `xl` (corrige aussi un débordement de 12 px). Montants (`formatAmount`) et `StatusBadge` en `whitespace-nowrap` dans les tableaux (Prestations, Paiements, Dépenses, Stock) et la carte Employée — le `DataTable` scrolle déjà (`overflow-x-auto`). Vérifié au navigateur (Playwright) : 10 pages × 3 formats iPad (768×1024, 820×1180, 1024×1366) sans débordement ni texte compressé, desktop 1440 inchangé (3 colonnes, Clients côte à côte) | `src/features/paiements/PaymentsSummaryCards.tsx`, `comptabilite/FinanceSummaryCards.tsx` + `ExpensesTable.tsx`, `marketing/MarketingSummaryCards.tsx`, `employes/EmployeesPerformanceGrid.tsx` + `EmployeePerformanceCard.tsx`, `clients/ClientsDirectory.tsx`, `prestations/ServicesTable.tsx`, `paiements/PaymentsTable.tsx`, `stock/StockTable.tsx`, `src/components/ui/StatusBadge.tsx` |

| 2026-07-11 | Assistant flottant (Lottie) | Cercle blanc flottant en bas à droite de **toutes les pages** (72 px, `fixed bottom-6 right-6 z-[44]` : au-dessus du contenu, sous le tiroir mobile z-45 et les modales z-50) : l'animation Lottie fournie par l'utilisateur tourne en boucle à l'intérieur (`lottie-react`, nouvelle dépendance), zoom léger au survol, enfoncement au clic. Le clic ouvre une modale « Assistant Zuri » (composant `Modal` existant) avec une 2ᵉ animation Lottie (`modal-animation.json`, balle violette) et un message d'accueil — UI seule. Un 1ᵉʳ fichier (`loading.json`) référençait 8 PNG externes non embarqués (image cassée) : remplacé par « Ball Animation 01 », 100 % vectorielle. Leçon : **vérifier qu'un Lottie n'a pas d'assets externes** (`"e":0` + `"p":"….png"`) avant de l'intégrer. Vérifié au navigateur (Playwright, 18 contrôles) : position, blanc en modes jour/nuit, animation vivante, ouverture/fermetures (clic, Échap, voile, ✕), présent sur `/agenda`, mobile 390×844 sans débordement. Spec : `docs/superpowers/specs/2026-07-11-assistant-launcher-design.md` | `src/features/assistant/` (`AssistantLauncher.tsx`, `animation.json`, `modal-animation.json`), `src/app/layout.tsx`, `package.json` (`lottie-react`) |

| 2026-07-11 | Chat de l'assistant Zuri | La modale de l'assistant devient un mini-chat : fil de bulles (Zuri à gauche en `bg-surface`, utilisatrice à droite en `bg-plum-800` blanc, coins galet), champ « Écrivez à Zuri… » arrondi + bouton envoyer rond (désactivé si vide), envoi par Entrée ou clic, champ vidé après envoi, défilement auto vers le dernier message (`max-h-64` scrollable). **UI de démonstration** : la réponse de Zuri est un message fixe après 600 ms — à brancher sur une vraie API d'assistant plus tard. L'animation Lottie de la modale passe en `size-28` au-dessus du fil. Vérifié au navigateur (Playwright, 16 contrôles, desktop 1440 + mobile 390) : accueil, envoi Entrée/bouton, réponse, pas de débordement | `src/features/assistant/` (`AssistantChat.tsx` nouveau, `AssistantLauncher.tsx`) |

| 2026-07-11 | Assistant en volet latéral (split view) | L'assistant quitte la modale centrée pour un **volet intégré à l'écran**, côté droit : sur desktop c'est une colonne du layout (`lg:sticky lg:top-0 lg:h-screen`, verre `glass-strong`, coins gauches `rounded-l-[2.5rem]` en miroir de la sidebar) — **le contenu de la page se compresse** (transition de largeur 300 ms, `w-0` ↔ `w-[26rem]`, même principe que le menu) ; sur mobile (< lg) c'est un tiroir fixé à droite avec voile prune cliquable. Effet verre corrigé après retour utilisateur : `glass-strong` (82 % opaque) → `glass` (16 %) + halos rose/prune floutés fixés **derrière** la vitre (comme la sidebar) — sans eux le backdrop-blur n'avait que le fond blanc à flouter et le verre paraissait opaque. Largeur intérieure fixe (26 rem, élargie de 22 rem sur retour utilisateur) pour que le texte soit révélé sans se recomposer pendant l'animation. En-tête : Lottie du cercle (l'animation « balle » de la modale a été retirée à la demande de l'utilisateur — `modal-animation.json` supprimé), titre + ✕ ; le chat remplit la hauteur (`flex-1 min-h-0`), champ de saisie toujours visible en bas. Le cercle flottant s'efface quand le volet est ouvert et revient à la fermeture (✕, Échap, voile mobile). Le composant `Modal` reste pour les 8 modales d'action. Vérifié au navigateur (Playwright, 14 contrôles) : compression 1182→832 px et restauration, pas de chevauchement, chat OK, mobile sans débordement | `src/features/assistant/` (`AssistantLauncher.tsx`, `AssistantChat.tsx`) |

| 2026-07-11 | Cercle assistant : Lottie plein cadre + bulle de dialogue animée | Retours utilisateur : 1) l'animation du cercle blanc passe en plein cadre (`size-full scale-125` + `overflow-hidden` sur le bouton — le zoom rogne la marge interne du fichier Lottie) ; 2) au-dessus du cercle, **petite bulle de dialogue animée** : bulle blanche avec queue orientée vers le cercle, 3 points prune « en train d'écrire » (keyframe `chat-dot`, décalés par `animation-delay`), flottement doux (`bubble-float` 2,8 s). Décorative (`aria-hidden`, `pointer-events-none`), elle s'efface avec le cercle quand le volet est ouvert. Vérifié au navigateur (Playwright, captures) | `src/features/assistant/AssistantLauncher.tsx`, `globals.css` (`animate-bubble-float`, `animate-chat-dot`) |

| 2026-07-11 | Menu replié = rail d'icônes | Sur desktop, le menu « fermé » ne disparaît plus (`lg:w-0`) : il se replie en **rail de 80 px** (`lg:w-20`) où restent visibles le logo Z et toutes les icônes, centrées, sans libellés (`lg:hidden` sur les spans) ; pilule rose active conservée en version icône, `title` natif en infobulle sur chaque icône. Prop `collapsed` ajoutée à `SidebarLink` et `ThemeToggle`. Le mobile ne change pas (tiroir : fermé = invisible). Vérifié au navigateur (Playwright, 6 contrôles) : 256 ↔ 80 px, 9 icônes visibles repliées, navigation depuis le rail, mobile intact | `src/components/layout/` (`Sidebar`, `SidebarLink`, `ThemeToggle`) |

| 2026-07-11 | Boutons d'en-tête responsive mobile | Sur mobile, le bouton d'action de l'en-tête (« Nouvelle prestation », « Nouveau rendez-vous »…) était écrasé à côté de la barre de recherche : libellé replié sur 2-3 lignes (boutons de 69-92 px de haut). Correctif : sous `sm` la zone recherche + action passe en **colonne** (recherche pleine largeur puis bouton pleine largeur, 47 px, une ligne) ; dès `sm` retour côte à côte. `PrimaryButton` gagne `justify-center` (libellé centré quand il est étiré) et `whitespace-nowrap` (ne se replie plus jamais). Vérifié au navigateur (Playwright, 390×844) : 8 pages à bouton d'action, une seule ligne partout, aucune débordement, modale toujours fonctionnelle, desktop 1440 inchangé | `src/components/layout/PageHeader.tsx`, `src/components/ui/PrimaryButton.tsx` |

### Notes techniques
- Toutes les données des écrans sont des **données de démonstration** dans `src/features/<feature>/data.ts` — à remplacer par une vraie API/BDD.
- Les types métier partagés (Payment, Client, Appointment, Service, Employee, StockItem, Campaign…) sont dans `src/types/index.ts`.
- Formatage des montants CFA / dates / pourcentages : `src/lib/format.ts`.
- Icônes : `lucide-react`. Les icônes étant des fonctions, tout composant qui les reçoit en props doit être un composant client (`"use client"` sur `Sidebar`).
